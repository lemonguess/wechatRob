from . import views
from django.urls import path


urlpatterns = [
    #    path('admin/', admin.site.urls),
    # path('email/verification/', views.EmailVerificationView.as_view()),
    # path('image/verification/<uuid:uuid>/', views.ImageVerificationView.as_view()),
    # path('register/', views.RegisterView.as_view()),
    # path('login/<uuid:uuid>/', views.LoginView.as_view()),
    # path('detail/', views.UserDetailView.as_view()),
    # path('portrait/upload/', views.PortraitView.as_view()),
    # path('shopping_car/', views.ShoppingCarView.as_view()),
]
